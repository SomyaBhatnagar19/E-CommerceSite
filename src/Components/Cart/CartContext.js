// import { createContext } from "react";

// export const CartContext = createContext({ cartItems: []});
import { createContext } from "react";

export const CartContext = createContext({ cartItems: [] });
